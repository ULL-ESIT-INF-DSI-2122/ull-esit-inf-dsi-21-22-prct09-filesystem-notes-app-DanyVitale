/**
 * Add describes the addition of two numbers.
 * @param a is the first number.
 * @param b is the second number.
 * @returns is the sum of a and b.
 */
export function add(a: number, b: number): number {
  return a + b;
}