/**
 * Add is a function that adds two numbers together.
 * @param a is a number
 * @param b is a number
 * @returns Returns the sum of a and b
 */
export function add(a: number, b: number) {
  return a + b;
}